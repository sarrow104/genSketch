#ifndef _${Target}_H_${timestamp}_
#define _${Target}_H_${timestamp}_

#include <QMainWindow>

class ${Target} : public QMainWindow
{
    Q_OBJECT;
public:
    explicit ${Target}(QWidget *parent = 0);

signals:

public slots:
};

#endif // _${Target}_H_${timestamp}_
